<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>
    <h1 class="m-3">Url Shortener</h1>
    <span class="m-3">Convert your url into short links</span>
    
<div class="m-3">
    <div class="mb-3">
     
        <form action="/url" method="post" onsubmit="reloadPage()">

            <label for="url" class="form-label">Enter the link</label>
            <input type="text" style="width: 50%;" name="url" class="form-control" id="url" placeholder="www.example.com">
            <input class="btn btn-primary mt-3" type="submit"/>
        </form>
      </div>
      <div>

    <% if (locals.message) { %>
     
   <p>Link Created</p>
  
        <% } %>
     
      </div>
      <div class="mb-3">
  

        <table class="table">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Link</th>
                <th scope="col">Short Link</th>
                <th scope="col">Clicks</th>
              </tr>
            </thead>
            <tbody>
            
            
              <% if (locals.data) { %>

                <% data.forEach((element,index) => { %>
                    <!-- <p> <%= element.redirectUrl %>  </p> -->

                    <tr>
                        <td scope="row" colspan="1"> 
                            <%= index+1 %>
                        </td>
                        <td scope="row" colspan="1"> 
                            <%= element.redirectUrl %>
                        </td>
                        <td colspan="1">
                            <%= `http://localhost:3000/url/${element.shortId}` %>
                        </td>

                        <td scope="row" colspan="1"> 
                            <%= element.visitHistory.length %>
                        </td>
                       
                      </tr>
                 
                <% }) %>
                 
                <% } %>
            </tbody>
          </table>
      </div>    
</div>

<script>
    function reloadPage() {
        window.location.href = "/url";
        return false; // Prevents the form from submitting normally
    }
</script>
    
</body>
</html> 